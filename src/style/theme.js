import { createTheme } from '@mui/system';

const theme = createTheme({
  palette: {
    primary: {
      main: '#bbdefb'

    },
    secondary: {
      main: '#717e71'
    }
  }
});
export default theme;
